import  'package:flutter/material.dart' ;


void main(){
  runApp(MyApp() ); // calling runing the app
} 

// Stateless Widget
class MyApp extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
  return MaterialApp(
    home : Scaffold(
      body: Text("Ani kuttan" ) //text
    ) //scaffold
  ) ; //Materialapp
  } 
} 
